import { defineConfig } from "astro/config";

export default defineConfig({
  // Set this to your deployed URL — RSS and canonical links depend on it.
  site: "https://noisefloor-beige.vercel.app",
});
